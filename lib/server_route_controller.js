// v1.2.1

Preloader = {
	// Dummy placeholder until we put something smart here
};

if ( typeof PreloadController === 'undefined' ) {
	PreloadController = RouteController.extend({
		// Dummy placeholder until we put something smart here
	});
}
